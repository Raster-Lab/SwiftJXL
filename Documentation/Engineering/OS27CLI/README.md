# SwiftJXL: OS 27 and CLI qualification

**Development candidate 2.1.0-dev.2, shared contract 0.4.0, 19 September 2026.** The owner authorised macOS/iOS 27 and corresponding Apple platforms, comprehensive command-line help, selectable verbosity and UNIX manuals before codec milestones. Active Apple minima are now **macOS, iOS/iPadOS, tvOS, visionOS and watchOS 27.0**. Swift tools 6.4 / Swift 6 language mode remain. Linux retains its independent Ubuntu 24.04 reference distribution and still needs native qualification.

The successor branch is `codex/swift-6-4-upgrade`, continuing [draft PR #5](https://github.com/Raster-Lab/SwiftJXL/pull/5) from [`64c5b50bfb6c3fa830a502ad887e6dcf867fa9a0`](https://github.com/Raster-Lab/SwiftJXL/commit/64c5b50bfb6c3fa830a502ad887e6dcf867fa9a0). That PR is stacked on the Milestone 1 draft. No merge, tag, release or codec algorithm implementation is included. Source and history in the legacy repositories are untouched. The preceding working tree was clean.

## Implemented changes

All nine active suite package manifests, independent examples, generated validation consumers, README/migration guides and agent instructions now use the new Apple baseline. The seven shared contract files are mirrored at revision 0.4.0 with [their digest manifest](../../COMMON_CONTRACT_SHA256.txt). The owner decision supersedes OS 26 constraints in the supplied documents; the [earlier report](../Swift64/README.md), its evidence, probes, supplied manifesto/supplement and original Milestone 1 evidence remain byte-for-byte historical records.

The library now calls the OS 27 `Swift.ByteOrder` overloads of bounded `RawSpan.load` and `OutputRawSpan.append`. Public API/lease/lifetime/precision semantics are unchanged. Sample geometry/capacity checks and exact two-byte output spans still exclude padding and initialise caller-owned uninitialised sample storage. No new image allocation/copy or compatibility branch was introduced. Existing golden-byte, odd-address, padded layout, precision, cancellation and ownership tests execute against these overloads. [FEATURES.md](FEATURES.md) reassesses F01–F13: OS availability no longer blocks eligible features, but unique containers, borrowing projections and async cleanup are only adopted when there is an actual workspace or lifecycle need. Missing SDK Optional/RigidArray declarations remain missing.

The new standalone executable **swiftjxl** depends only on SwiftJXL. It implements global/command help, version and truthful capability reporting (plain text or JSON). Reserved codec verbs report unavailability with exit 4 before opening any input/output; they do not simulate successful compression, validation or transcoding. CLI main functions stay outside library tests. The package remains free of external SwiftPM dependencies, and library-only consumers do not link the CLI. MIT licensing remains unchanged.

[CLI.md](../../../CLI.md) documents the executable and installation. `-h`, `--help`, `help [command]`, no-argument help and command-local help explain availability, syntax, defaults, examples, streams and exits. Five cumulative verbosity levels are accepted through `-v` repeats, `-vv`…`-vvvvv`, `--verbose 1`…`5`, `--verbose=+++` and `-verbose: 3`. Numeric/plus values set a level; bare flags increment. Invalid levels, quiet/verbose conflicts and invalid syntax fail with exit 2. Diagnostics use stderr; capability JSON remains clean stdout. Errors survive quiet mode; payloads, metadata, raw addresses and file paths are not logged. A closed stdout pipe returns 6 instead of a success or uncontrolled SIGPIPE termination.

[ManPages/swiftjxl.1](../../../ManPages/swiftjxl.1) is installed/updated with the matching executable by [Scripts/install-cli.sh](../../../Scripts/install-cli.sh). The installer builds release code by default or checks a prebuilt binary against VERSION, checks the manual's version header, prepares both files before replacing either, supports absolute prefixes and DESTDIR, sets modes 0755/0644 and refuses destination symlinks/directories. Each file rename is atomic; no cross-file transactional guarantee is claimed. Custom-prefix lookup uses `man -M PREFIX/share/man swiftjxl` or MANPATH. No global CLI installation was present on PATH at inventory; tests use temporary staging, not a system-wide installation. Future packaging recipes must include both files.

## Local environment and executed validation

Xcode **27.0 / 27A266a**, Apple Swift **6.4 / swiftlang-6.4.0.34.1**, Clang **2100.3.34.1**; SDKs **27.0**; Apple **M5 Max** on macOS **27.0 build 26A428**. This now supplies an actual minimum-OS macOS arm64 execution. The global Xcode selection is unchanged; commands select `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`. [Environment records](Evidence/Environment) retain exact commands and power/runtime inventory. Only iOS 17.4 simulator runtime is installed, so iOS/tvOS/visionOS/watchOS 27 runtime tests remain unavailable. Native Intel/Linux and physical-device tests remain unexecuted.

[Candidate/report.json](Evidence/Candidate/report.json) contains every Swift Build argument, exit code, discovery result, test XML/log, selected checks and source/test/script hashes. The executed runner command is `bash Scripts/validate.sh --checks all --jobs 2 --repetitions 5 --disable-package-sandbox --output <new-directory>`. Clean/incremental debug/release builds, local independent consumption and separate sanitizers passed. No tests were skipped.

| Run | Discovered declarations | Passed declarations | Passed case executions | Failed / skipped declarations |
| --- | ---: | ---: | ---: | ---: |
| debug | 31 | 31 | 32 | 0 / 0 |
| release | 31 | 31 | 32 | 0 / 0 |
| repetition | 7 | 7 | 40 | 0 / 0 |
| asan | 31 | 31 | 32 | 0 / 0 |
| tsan | 31 | 31 | 32 | 0 / 0 |

Parameterised arguments and fixed repetitions increase case executions relative to declarations. Repetition targets ownership/cancellation tests and does not retry until success. The [four-module synthetic harness](Evidence/Harness) also passed debug, release, ASan and TSan: exact 12/16-bit padded storage, retained allocation identity, one pixel allocation, zero adapter copies, concurrent readers, single-writer authority and lease mapping. These are storage checks, not compressed-format interoperability.

Direct headless **Xcode package tests passed 31 declarations / 32 case executions**, with zero failures/skips. The correct scheme is now **`SwiftJXL-Package`** because adding an executable product moved the test action from the former library-product scheme. [Initial scheme failures](Evidence/XcodeInitialScheme) are preserved alongside [successful commands/results](Evidence/Xcode). Xcode also built the **`swiftjxl`** scheme in Release and ran its version and JSON capability commands successfully. No hand-maintained Xcode project is required.

[SDK compilation](Evidence/Platforms/results.json) passed ten library compile/link targets for this repository: macOS arm64/x86_64; iOS, tvOS and visionOS arm64 device/simulator; watchOS arm64_32 device and arm64 simulator, all at OS 27.0. Across four repositories this is **40/40**. Compilation does not close the non-macOS runtime gates.

The release binary passed **109 black-box process checks** in [CLI/report.json](Evidence/CLI/report.json). The suite total is **432** checks. These include expected exit-2/4/6 cases, rather than claiming every process should exit zero. Tests cover all verbosity forms/levels and invalid values, help equivalence, JSON/stderr separation, private-path nonlogging, reserved commands without output modification, deterministic broken pipes, staged install/update with spaces, stale/wrong binary/manual refusal, symlink refusal, file modes, `man -M` lookup, mandoc lint and rendered content. [Manual rendering](Evidence/CLI/manual-plain.txt) was inspected. The [default build-and-install path](Evidence/InstallerBuild) separately succeeded in a staged `/usr/local` prefix for all four tools.

The full library validation preceded a final installer guard against stale source manual versions. Only `Scripts/install-cli.sh` and `Scripts/test-cli.py` subsequently changed; the final CLI checks were rerun and hash both scripts. **No library, CLI Swift source, unit test or core manifest changed after full validation.** [FINAL_INPUTS.json](Evidence/FINAL_INPUTS.json) records that distinction and the complete current fingerprints. Earlier manifest-cache permission errors, man formatting corrections and a rendered-overstrike assertion correction are retained in [InitialChecks](Evidence/InitialChecks); no failing candidate test was silently counted as passed.

## Performance and supply-chain limits

[Benchmark evidence](Evidence/Benchmark) compares the preceding Swift 6.4 development source to this OS 27 change through the shared Image.swift implementation in SwiftJ2K. Both consumers target macOS 27 and use the same compiler; the dependency's prior floor was 26, so this is a source/deployment change comparison, not compiler performance. Synthetic little-endian 16-bit padded 64×63, 512×511 and 2048×2047 images receive five warmups and twenty interleaved observations per case, then a repeat using the same executables. Raw samples, median/p95/spread, checksums, thermal state, process RSS/footprint and executable sizes are retained.

First-run write median changes were +1.19%, −4.34%, +0.70%; total median changes +2.34%, +2.34%, −1.47%. A +5.58% tiny-image allocation timing crossed the pre-existing investigation threshold; the repeat changed that phase by −11.74%, so this crossing did not reproduce. Repeat write changes were +2.14%, −0.06%, +3.33%; total changes +1.53%, −0.53%, +0.20%. Process RSS/footprint rose less than 5% initially and decreased on repeat. The representative consumer executable changed from 325,328 to 324,768 bytes. Thermal raw state was 0 during both runs; frequency and scheduling were not laboratory-controlled. These measurements do not establish a general speedup. The earlier roughly 6.5% tiny-write regression against the pre-span baseline remains an open stable-release item; this follow-up comparison does not silently waive it.

Build-associated SPDX and CycloneDX generation succeeded for **both library and CLI products**, sixteen emitted files suite-wide. [Library SBOMs](Evidence/Candidate/sboms), [CLI SBOMs](Evidence/CLISBOM) and [field/provenance audits](Evidence/SBOMAudit) retain originals. The same installed generator schema-bundle issue and SPDX specVersion/externalUrl / duplicate CycloneDX identifiers remain. JSON parsing and hash checks pass; complete schema/semantic conformance is unqualified. See the [prior specification-backed audit](../Swift64/Evidence/SBOMAudit/RESULTS.md). Native SDK/runtime components are outside that SwiftPM graph. No dependency/licence or codec support claim is inferred from SBOM generation.

## Handover and rollback

[Evidence/SHA256SUMS.txt](Evidence/SHA256SUMS.txt) covers text artefacts; [provenance](Evidence/PROVENANCE.json) distinguishes exact copies, derived subsets and hardware-ID redactions. Untouched local Xcode bundles and raw logs remain outside the public text archive. No secrets/patient fixtures or downloaded implementation were used. Source, test, scripts, versions and manuals are pinned by final fingerprints. A fresh published-revision URL consumer is recorded separately after publication; library independence is already exercised locally.

Rollback restores the entire prior source at `64c5b50bfb6c3fa830a502ad887e6dcf867fa9a0` together with its matching platform floor, scripts and documentation. Reducing only the new manifest floor to 26 is not valid because production now uses OS 27 APIs. Requested disposition is review of this bounded baseline/CLI change; no human approval or stable readiness is fabricated. Remaining work includes the existing codec milestones, native platform/device qualification, SBOM conformance and the retained performance release check.
